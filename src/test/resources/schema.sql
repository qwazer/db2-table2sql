create table t1 (
  username varchar(256),
  password varchar(256),
  enabled boolean
);

create table t2 (
  username varchar(256),
  authority varchar(256)
);

create table t3 (
  c1 varchar(256),
  c2 varchar(256)
);